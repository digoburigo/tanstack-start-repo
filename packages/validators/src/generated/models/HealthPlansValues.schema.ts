/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    value: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
}
).strict();
const relationSchema = z.object({
    healthPlans: z.array(z.unknown()).optional(),
}
);

/**
 * `HealthPlansValues` schema excluding foreign keys and relations.
 */
export const HealthPlansValuesScalarSchema = baseSchema;


/**
 * `HealthPlansValues` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const HealthPlansValuesSchema = HealthPlansValuesScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const HealthPlansValuesPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const HealthPlansValuesPrismaUpdateSchema = z.object({
    id: z.string(),
    value: z.string(),
    createdAt: z.coerce.date().default(() => new Date())
}).partial().passthrough();


/**
 * `HealthPlansValues` schema for create operations excluding foreign keys and relations.
 */
export const HealthPlansValuesCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true
});


/**
 * `HealthPlansValues` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const HealthPlansValuesCreateSchema = baseSchema.partial({
    id: true, createdAt: true
});


/**
 * `HealthPlansValues` schema for update operations excluding foreign keys and relations.
 */
export const HealthPlansValuesUpdateScalarSchema = baseSchema.partial();


/**
 * `HealthPlansValues` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const HealthPlansValuesUpdateSchema = HealthPlansValuesUpdateScalarSchema;

