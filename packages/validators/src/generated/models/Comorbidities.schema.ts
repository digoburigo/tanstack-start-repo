/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    type: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false),
}
).strict();
const relationSchema = z.object({
    createdByUser: z.record(z.unknown()).optional(),
    updatedByUser: z.record(z.unknown()).optional(),
    deletedByUser: z.record(z.unknown()).optional(),
    organization: z.record(z.unknown()).optional(),
    patient: z.record(z.unknown()).optional(),
    comorbiditiesValues: z.record(z.unknown()),
    appointment: z.record(z.unknown()).optional(),
}
);
const fkSchema = z.object({
    createdById: z.string().nullish(),
    updatedById: z.string().nullish(),
    deletedById: z.string().nullish(),
    organizationId: z.string().nullish(),
    patientId: z.string().nullish(),
    comorbiditiesValuesId: z.string(),
    appointmentId: z.string().nullish(),
}
);

/**
 * `Comorbidities` schema excluding foreign keys and relations.
 */
export const ComorbiditiesScalarSchema = baseSchema.omit({
    deleted: true,
});


/**
 * `Comorbidities` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const ComorbiditiesSchema = ComorbiditiesScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const ComorbiditiesPrismaCreateSchema = baseSchema.omit({ type: true }).partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const ComorbiditiesPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false)
}).partial().passthrough();


/**
 * `Comorbidities` schema for create operations excluding foreign keys and relations.
 */
export const ComorbiditiesCreateScalarSchema = baseSchema.omit({ type: true }).partial({
    id: true, createdAt: true, updatedAt: true, deleted: true
});


/**
 * `Comorbidities` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const ComorbiditiesCreateSchema = ComorbiditiesCreateScalarSchema.merge(fkSchema);


/**
 * `Comorbidities` schema for update operations excluding foreign keys and relations.
 */
export const ComorbiditiesUpdateScalarSchema = baseSchema.omit({ type: true }).partial();


/**
 * `Comorbidities` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const ComorbiditiesUpdateSchema = ComorbiditiesUpdateScalarSchema.merge(fkSchema.partial());

