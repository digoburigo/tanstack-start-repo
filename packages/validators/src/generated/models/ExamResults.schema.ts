/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    type: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false),
    result: z.string(),
    date: z.coerce.date(),
}
).strict();
const relationSchema = z.object({
    createdByUser: z.record(z.unknown()).optional(),
    updatedByUser: z.record(z.unknown()).optional(),
    deletedByUser: z.record(z.unknown()).optional(),
    organization: z.record(z.unknown()).optional(),
    patient: z.record(z.unknown()),
    examResultsValues: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    createdById: z.string().nullish(),
    updatedById: z.string().nullish(),
    deletedById: z.string().nullish(),
    organizationId: z.string().nullish(),
    patientId: z.string(),
    examResultsValuesId: z.string(),
}
);

/**
 * `ExamResults` schema excluding foreign keys and relations.
 */
export const ExamResultsScalarSchema = baseSchema.omit({
    deleted: true,
});


/**
 * `ExamResults` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const ExamResultsSchema = ExamResultsScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const ExamResultsPrismaCreateSchema = baseSchema.omit({ type: true }).partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const ExamResultsPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false),
    result: z.string(),
    date: z.coerce.date()
}).partial().passthrough();


/**
 * `ExamResults` schema for create operations excluding foreign keys and relations.
 */
export const ExamResultsCreateScalarSchema = baseSchema.omit({ type: true }).partial({
    id: true, createdAt: true, updatedAt: true, deleted: true
});


/**
 * `ExamResults` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const ExamResultsCreateSchema = ExamResultsCreateScalarSchema.merge(fkSchema);


/**
 * `ExamResults` schema for update operations excluding foreign keys and relations.
 */
export const ExamResultsUpdateScalarSchema = baseSchema.omit({ type: true }).partial();


/**
 * `ExamResults` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const ExamResultsUpdateSchema = ExamResultsUpdateScalarSchema.merge(fkSchema.partial());

