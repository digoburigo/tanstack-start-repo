/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    email: z.string(),
    role: z.string().nullish(),
    status: z.string(),
    expiresAt: z.coerce.date(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
}
).strict();
const relationSchema = z.object({
    organization: z.record(z.unknown()),
    user: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    organizationId: z.string(),
    inviterId: z.string(),
}
);

/**
 * `Invitation` schema excluding foreign keys and relations.
 */
export const InvitationScalarSchema = baseSchema;


/**
 * `Invitation` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const InvitationSchema = InvitationScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const InvitationPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const InvitationPrismaUpdateSchema = z.object({
    id: z.string(),
    email: z.string(),
    role: z.string().nullish(),
    status: z.string(),
    expiresAt: z.coerce.date(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish()
}).partial().passthrough();


/**
 * `Invitation` schema for create operations excluding foreign keys and relations.
 */
export const InvitationCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Invitation` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const InvitationCreateSchema = InvitationCreateScalarSchema.merge(fkSchema);


/**
 * `Invitation` schema for update operations excluding foreign keys and relations.
 */
export const InvitationUpdateScalarSchema = baseSchema.partial();


/**
 * `Invitation` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const InvitationUpdateSchema = InvitationUpdateScalarSchema.merge(fkSchema.partial());

