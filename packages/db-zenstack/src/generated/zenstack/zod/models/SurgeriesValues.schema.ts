/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    value: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
}
).strict();
const relationSchema = z.object({
    surgeries: z.array(z.unknown()).optional(),
}
);

/**
 * `SurgeriesValues` schema excluding foreign keys and relations.
 */
export const SurgeriesValuesScalarSchema = baseSchema;


/**
 * `SurgeriesValues` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const SurgeriesValuesSchema = SurgeriesValuesScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const SurgeriesValuesPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const SurgeriesValuesPrismaUpdateSchema = z.object({
    id: z.string(),
    value: z.string(),
    createdAt: z.coerce.date().default(() => new Date())
}).partial().passthrough();


/**
 * `SurgeriesValues` schema for create operations excluding foreign keys and relations.
 */
export const SurgeriesValuesCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true
});


/**
 * `SurgeriesValues` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const SurgeriesValuesCreateSchema = baseSchema.partial({
    id: true, createdAt: true
});


/**
 * `SurgeriesValues` schema for update operations excluding foreign keys and relations.
 */
export const SurgeriesValuesUpdateScalarSchema = baseSchema.partial();


/**
 * `SurgeriesValues` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const SurgeriesValuesUpdateSchema = SurgeriesValuesUpdateScalarSchema;

