/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    type: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false),
}
).strict();
const relationSchema = z.object({
    createdByUser: z.record(z.unknown()).optional(),
    updatedByUser: z.record(z.unknown()).optional(),
    deletedByUser: z.record(z.unknown()).optional(),
    organization: z.record(z.unknown()).optional(),
    appointment: z.record(z.unknown()),
    cid: z.record(z.unknown()),
}
);
const fkSchema = z.object({
    createdById: z.string().nullish(),
    updatedById: z.string().nullish(),
    deletedById: z.string().nullish(),
    organizationId: z.string().nullish(),
    appointmentId: z.string(),
    cidId: z.string(),
}
);

/**
 * `AppointmentCid` schema excluding foreign keys and relations.
 */
export const AppointmentCidScalarSchema = baseSchema.omit({
    deleted: true,
});


/**
 * `AppointmentCid` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const AppointmentCidSchema = AppointmentCidScalarSchema.merge(fkSchema).merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const AppointmentCidPrismaCreateSchema = baseSchema.omit({ type: true }).partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const AppointmentCidPrismaUpdateSchema = z.object({
    id: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date(),
    deletedAt: z.coerce.date().nullish(),
    deleted: z.boolean().default(false)
}).partial().passthrough();


/**
 * `AppointmentCid` schema for create operations excluding foreign keys and relations.
 */
export const AppointmentCidCreateScalarSchema = baseSchema.omit({ type: true }).partial({
    id: true, createdAt: true, updatedAt: true, deleted: true
});


/**
 * `AppointmentCid` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const AppointmentCidCreateSchema = AppointmentCidCreateScalarSchema.merge(fkSchema);


/**
 * `AppointmentCid` schema for update operations excluding foreign keys and relations.
 */
export const AppointmentCidUpdateScalarSchema = baseSchema.omit({ type: true }).partial();


/**
 * `AppointmentCid` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const AppointmentCidUpdateSchema = AppointmentCidUpdateScalarSchema.merge(fkSchema.partial());

