/******************************************************************************
 * This file was generated by ZenStack CLI.
 ******************************************************************************/

/* eslint-disable */
// @ts-nocheck

import { z } from 'zod';
const baseSchema = z.object({
    id: z.string(),
    code: z.string(),
    description: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish(),
}
).strict();
const relationSchema = z.object({
    appointmentCid: z.array(z.unknown()).optional(),
}
);

/**
 * `Cid` schema excluding foreign keys and relations.
 */
export const CidScalarSchema = baseSchema;


/**
 * `Cid` schema including all fields (scalar, foreign key, and relations) and validations.
 */
export const CidSchema = CidScalarSchema.merge(relationSchema.partial());


/**
 * Schema used for validating Prisma create input. For internal use only.
 * @private
 */
export const CidPrismaCreateSchema = baseSchema.partial().passthrough();


/**
 * Schema used for validating Prisma update input. For internal use only.
 * @private
 */
export const CidPrismaUpdateSchema = z.object({
    id: z.string(),
    code: z.string(),
    description: z.string(),
    createdAt: z.coerce.date().default(() => new Date()),
    updatedAt: z.coerce.date().nullish()
}).partial().passthrough();


/**
 * `Cid` schema for create operations excluding foreign keys and relations.
 */
export const CidCreateScalarSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Cid` schema for create operations including scalar fields, foreign key fields, and validations.
 */
export const CidCreateSchema = baseSchema.partial({
    id: true, createdAt: true, updatedAt: true
});


/**
 * `Cid` schema for update operations excluding foreign keys and relations.
 */
export const CidUpdateScalarSchema = baseSchema.partial();


/**
 * `Cid` schema for update operations including scalar fields, foreign key fields, and validations.
 */
export const CidUpdateSchema = CidUpdateScalarSchema;

